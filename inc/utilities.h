#ifndef UTILS
#define UTILS
#include <string>
#include <list>
#include <fstream>
#include <sstream>
#include <iostream>

using namespace std;


bool is_hexadecimal(const string& s);
string readFileToString(const string& filename);

#endif